server:
  port: 8090
spring:
  application:
    name: students
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://host.docker.internal:5432/students
    username: postgres
    password: Root@123
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
